# Images

- **In this folder (repo / Vercel):** Only **banners/** (hero images), **og-image.jpg**, and **logo SVGs** (e.g. `mauritius-explored-logo.svg`). Keep these here; they are not in Supabase.
- **In Supabase only:** All other content images (beaches, places-to-visit, activities, itinerary, mosaic, etc.). See `docs/IMAGES.md`.
